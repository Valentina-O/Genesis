info:
  name: Registar Usuario
  type: http
  seq: 1

http:
  method: POST
  url: http://localhost:8080/api/v1/auth/registrar
  body:
    type: json
    data: |-
      {
        "usuario": "valentina",
        "correo": "vale@correo.com",
        "contrasena": "123456"
      }
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
