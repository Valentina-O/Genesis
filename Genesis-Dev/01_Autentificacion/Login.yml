info:
  name: Login
  type: http
  seq: 2

http:
  method: POST
  url: "{{base_url}}/auth/login"
  body:
    type: json
    data: |
      {
        "usuario": "admin",
        "contrasena": "123456"
      }
  auth: inherit

runtime:
  scripts:
    - type: after-response
      code: |-
        // En la pestaña 'Scripts' -> 'After Response' del Login
        const data = res.getBody();
        if (data.token) {
          bru.setEnvVar("jwt_token", data.token);
        }

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
