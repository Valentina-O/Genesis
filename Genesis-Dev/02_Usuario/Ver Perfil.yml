info:
  name: Ver Perfil
  type: http
  seq: 1

http:
  method: GET
  url: "{{base_url}}/usuario/perfil"
  auth:
    type: bearer
    token: "{{jwt_token}}"

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
